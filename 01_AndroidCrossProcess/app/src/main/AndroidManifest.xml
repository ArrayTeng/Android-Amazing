<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tengfei.androidcrossprocess">

    <permission
        android:name="com.example.tengfei.androidcrossprocess.aidldemo.BookManagerService"
        android:protectionLevel="normal" />
    <uses-permission android:name="com.example.tengfei.androidcrossprocess.aidldemo.BookManagerService" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".messenger.MessengerActivity" />
        <activity android:name=".aidldemo.AIDLActivity" />

        <activity android:name=".blog.FirstActivity"/>

        <activity android:name=".blog.SecondActivity" android:process=":remote"/>
        <activity android:name=".blog.ThirdActivity" android:process="com.example.tengfei.androidcrossprocess.blog.ThirdActivity"/>
        <activity android:name=".blog.binderpool.BinderPoolActivity" />

        <service
            android:name=".messenger.MessengerService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" />
        <service
            android:name=".aidldemo.BookManagerService"
            android:enabled="true"
            android:exported="true"
            android:process="com.example.tengfei.androidcrossprocess.aidldemo.BookManagerService" />
        <service android:name=".blog.binderpool.BinderPoolService" />
    </application>

</manifest>